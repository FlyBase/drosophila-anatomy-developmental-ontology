PREFIX owl: <http://www.w3.org/2002/07/owl#>

SELECT ?cls WHERE {
	?cls a owl:Class .
	FILTER (strstarts(str(?cls), "http://purl.obolibrary.org/obo/FBbt_") &&
		! regex(str(?cls), "^http://purl.obolibrary.org/obo/FBbt_[0-9]{8}$"))
	FILTER NOT EXISTS { ?cls owl:deprecated ?v }
}
